{
  "name": "@lottiefiles/design-tokens",
  "version": "1.0.0",
  "description": "Design Tokens for LottieFiles Design Systems",
  "main": "build.js",
  "scripts": {
    "build": "node_modules/.bin/style-dictionary build",
    "clean": "rm -rf build",
    "watch": "npm run build && chokidar  \"tokens/**/*.json\" -c \"npm run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LottieFiles Product Designers and Engineers",
  "license": "Apache-2.0",
  "devDependencies": {
    "chokidar": "^3.5.3",
    "style-dictionary": "latest"
  }
}
